# ARV - Платформа для создания AR-контента

B2B SaaS платформа для создания AR-контента на основе распознавания изображений (NFT markers).

## 🏗️ Архитектура

Проект построен на современной архитектуре с использованием следующих компонентов:

- **Backend**: FastAPI - высокопроизводительный Python фреймворк для создания API и HTML-рендеринга
- **Frontend**: HTML + Jinja2 + htmx + Alpine.js - легковесный серверный рендеринг
- **База данных**: SQLite (разработка) / PostgreSQL (продакшен)
- **AR Engine**: MindAR 1.1.5 + Three.js 0.147.0 - движок для распознавания маркеров и отображения AR-контента

## 🛠️ Технологический стек

### Backend & Frontend (Full-stack)
- Python 3.11+
- FastAPI 0.109.0 (API + HTML-рендеринг)
- SQLAlchemy 2.0.41 (асинхронный ORM)
- AsyncPG 0.31.0 (асинхронный драйвер PostgreSQL)
- Alembic 1.13.1 (миграции базы данных)
- Pydantic 2.10.4 (валидация данных)
- Uvicorn 0.27.0 (ASGI сервер)

### Frontend Technologies
- **Templates**: Jinja2 (серверный рендеринг)
- **Dynamic Updates**: htmx (AJAX через HTML-атрибуты)
- **Local State**: Alpine.js (компонент-like поведение)
- **Styling**: Tailwind CSS (utility-first CSS)
- **Icons**: Material Icons (CDN)
- **No build process required** - всё работает через CDN

### AR/VR
- MindAR 1.1.5 (движок AR, image tracking)
- Three.js 0.147.0 (3D графика, WebGL рендеринг)
- TensorFlow.js (WebGL backend для трекинга)

### Инфраструктура
- Docker & Docker Compose
- PostgreSQL 15
- Redis (планируется)
- Nginx (в будущем)

## 🚀 Быстрый старт

Всего 3 шага до запуска рабочего приложения:

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/fegerV/ARV
cd ARV
```

### 2. Настройте переменные окружения
```bash
cp .env.example .env
# При необходимости отредактируйте .env файл
```

### 3. Запустите приложение

#### Вариант A: Через Docker Compose (рекомендуется)
```bash
docker compose up -d
```

#### Вариант B: Локальный запуск
```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt

# Примените миграции
alembic upgrade head

# Запустите сервер
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Вариант C: Через скрипт (Windows)
```bash
quick_start.bat
```

После запуска:
- API и Админ панель: http://localhost:8000
- API Документация: http://localhost:8000/docs
- Админ-панель: http://localhost:8000/admin

### Данные для входа по умолчанию
- **Email**: admin@vertexar.com
- **Пароль**: admin123

⚠️ **ВАЖНО**: Сразу после первого запуска измените пароль администратора!

## 📁 Структура проекта

```
ARV/
├── app/                    # Backend FastAPI приложение
│   ├── api/               # API маршруты
│   │   └── routes/        # Отдельные модули маршрутов
│   ├── core/              # Конфигурация, БД, безопасность
│   ├── models/            # SQLAlchemy модели
│   ├── schemas/           # Pydantic схемы
│   ├── services/          # Бизнес-логика
│   ├── html/              # HTML-маршруты и шаблоны
│   └── utils/             # Вспомогательные функции
├── templates/             # HTML шаблоны (Jinja2)
│   ├── components/        # Переиспользуемые компоненты
│   ├── auth/              # Аутентификация
│   ├── companies/         # Управление компаниями
│   ├── projects/          # Управление проектами
│   ├── ar-content/        # AR контент
│   ├── notifications/     # Уведомления
│   └── settings/          # Настройки
├── static/                # Статические файлы
├── alembic/               # Миграции базы данных
├── storage/               # Локальное хранилище контента
│   └── VertexAR/         # AR контент (новая структура)
│       └── {project_name}/
│           └── {order_number}/
├── tests/                 # Тесты
├── scripts/               # Вспомогательные скрипты
│   ├── checks/           # Скрипты проверки
│   ├── deploy/           # Скрипты развертывания
│   ├── test_data/        # Создание тестовых данных
│   └── servers/           # Скрипты запуска серверов
├── docs/                  # Документация
├── docker-compose.yml     # Конфигурация Docker Compose
├── Dockerfile             # Production Dockerfile
├── Dockerfile.dev         # Development Dockerfile
├── requirements.txt       # Python зависимости
└── README.md              # Основная документация
```

## 📂 Структура хранения файлов

### Новая структура (текущая)
```
storage/
└── VertexAR/
    └── {project_name}/
        └── {order_number}/
            ├── photo.{ext}
            ├── video.{ext}
            ├── qr_code.png
            ├── thumbnail.webp
            └── marker.mind
```

**Пример:**
```
storage/VertexAR/Портреты/ORD-20260125-4492/
```

### Старая структура (поддерживается для обратной совместимости)
```
storage/content/VertexAR/Posters/{company_id}_{company_name}/{project_name}/{order_number}/
```

## 🛠️ Команды разработки

### Полный стек (Backend + Frontend)
```bash
# Установка зависимостей
pip install -r requirements.txt

# Локальный запуск (включая HTML-рендеринг)
uvicorn app.main:app --reload --port 8000

# Запуск тестов
pytest tests/ -v --cov=app

# Применение миграций
alembic upgrade head

# Создание новой миграции
alembic revision --autogenerate -m "Описание изменений"
```

### Скрипты обслуживания
```bash
# Проверки системы
python scripts/checks/final_admin_check.py
python scripts/checks/check_ar_content_pages.py

# Тестовый сервер
python scripts/servers/run_admin_test_server.py

# Создание тестовых данных
python scripts/test_data/create_sample_data.py
```

### Разработка шаблонов
```bash
# Шаблоны находятся в /templates/
# Используют Jinja2, htmx, Alpine.js, Tailwind CSS
# Нет необходимости в сборке - изменения применяются сразу

# Горячая перезагрузка работает автоматически с uvicorn --reload
```

### Docker
```bash
# Запуск всех сервисов
docker compose up -d

# Перезапуск сервисов
docker compose restart

# Просмотр логов
docker compose logs -f app

# Запуск команды внутри контейнера
docker compose exec app bash
```

## 📚 Документация

Дополнительная документация находится в папке `/docs`:

### Основная документация
- **[API Reference](docs/API.md)** - Полная документация API
- **[API Examples](docs/API_EXAMPLES.md)** - Примеры использования API
- **[Deployment Guide](docs/DEPLOYMENT.md)** - Руководство по развертыванию
- **[Architecture](docs/ARCHITECTURE.md)** - Архитектура системы
- **[Data Models](docs/DATA_MODELS.md)** - Модели данных и схема БД
- **[Storage System](docs/STORAGE.md)** - Система хранения файлов

### Техническая документация
- **[Authentication System](docs/AUTH_SYSTEM.md)** - Документация по системе аутентификации
- **[Migrations](docs/MIGRATIONS.md)** - Документация по миграциям базы данных
- **[MindAR Generator](docs/MINDAR_GENERATOR.md)** - Документация по генератору AR-маркеров
- **[AR Viewer Troubleshooting](docs/AR_VIEWER_TROUBLESHOOTING.md)** - Диагностика проблем AR viewer
- **[Tech Stack](docs/TECH_STACK.md)** - Подробное описание технологического стека

### Руководства
- **[Manual Testing Guide](docs/MANUAL_TESTING_GUIDE.md)** - Руководство по ручному тестированию
- **[Troubleshooting](docs/TROUBLESHOOTING.md)** - Решение распространенных проблем

### Продвинутая документация
- **[Security](docs/SECURITY.md)** - Политика безопасности для продакшена
- **[Performance](docs/PERFORMANCE.md)** - Оптимизация производительности
- **[Services](docs/SERVICES.md)** - Описание сервисов системы

Также доступна интерактивная документация API по адресу: http://localhost:8000/docs

## 📊 Статус проекта

- **Версия**: 2.0.0
- **Статус**: Активная разработка
- **Лицензия**: Proprietary (Все права защищены)

### Текущие возможности
- [x] Управление пользователями и ролями
- [x] Управление компаниями и проектами
- [x] Создание и управление AR-контентом
- [x] Хранение и управление медиафайлами
- [x] Генерация AR-маркеров (MindAR)
- [x] Генерация превью и миниатюр
- [x] Аутентификация JWT
- [x] OAuth интеграции (Yandex и другие)
- [x] API документация (Swagger/OpenAPI)
- [x] Docker контейнеризация
- [x] Автоматические миграции
- [x] Система уведомлений
- [x] Аналитика и статистика
- [x] Режимы воспроизведения видео (ручной, последовательный, циклический)
- [x] Автоматическое переключение видео в AR viewer

## 📱 AR Viewer

AR контент доступен по публичным ссылкам вида:
```
https://your-domain.com/view/{unique_id}
```

### Поддерживаемые устройства

| Устройство | Браузер | Статус |
|------------|---------|--------|
| Компьютер + веб-камера | Chrome, Firefox | ✅ Рекомендуется |
| iPhone | Safari | ✅ Хорошо работает |
| Android | Chrome | ⚠️ Зависит от GPU |

### Технические требования
- **WebGL** — обязателен (CPU fallback не поддерживается)
- **Камера** — доступ через getUserMedia
- **HTTPS** — обязателен для доступа к камере

### Диагностика
Добавьте `?diagnose=1` к URL для детальных логов:
```
https://your-domain.com/view/{unique_id}?diagnose=1
```

Подробнее: [AR Viewer Troubleshooting](docs/AR_VIEWER_TROUBLESHOOTING.md)

### Планы на будущее
- [ ] WebSocket подключения для уведомлений в реальном времени
- [ ] Redis для кэширования и фоновых задач
- [ ] Мониторинг и логирование (Prometheus, Grafana)
- [ ] Интеграция с облачным хранилищем (S3, Yandex Object Storage)
- [ ] Мобильное приложение
- [ ] Расширенные аналитики и отчеты
- [ ] Многоязычная поддержка

## ⚙️ Требования

Для запуска проекта необходимы следующие компоненты:

- **Python** 3.11+ (для backend разработки)
- **PostgreSQL** 12+ (или Docker для контейнеризации)
- **Docker** и **Docker Compose** (рекомендуется)
- **Git** для контроля версий
- **Node.js** 18+ (для генерации AR-маркеров через MindAR)

## 🔐 Безопасность

### Аутентификация
- JWT токены с настраиваемым временем жизни
- Rate limiting (5 попыток за 15 минут)
- Блокировка аккаунта после превышения лимита
- Валидация сложных паролей (8+ символов, uppercase, lowercase, digits)
- SHA-256 хеширование паролей

### Защита API
- Все API endpoints требуют аутентификации
- CORS настроен для разрешенных доменов
- Валидация входных данных через Pydantic
- Защита от SQL-инъекций через SQLAlchemy ORM

## 🧪 Тестирование

### Юнит-тесты
```bash
# Запуск всех юнит-тестов
pytest tests/unit/

# Запуск юнит-тестов с покрытием
pytest tests/unit/ --cov=app --cov-report=html
```

### Интеграционные тесты
```bash
# Запуск всех интеграционных тестов
pytest tests/integration/
```

### Запуск всех тестов
```bash
# Запуск всех тестов (юнит и интеграционные)
pytest tests/ -v

# С покрытием кода
pytest tests/ -v --cov=app --cov-report=html
```

## 🚀 Развертывание

### Конфигурация для продакшена

Приложение настроено для работы в продакшене с помощью переменных окружения. Ключевые настройки продакшена включают:

- `ENVIRONMENT=production` - Устанавливает приложение в режим продакшена
- `DEBUG=false` - Отключает режим отладки в продакшене
- Правильные настройки CORS для вашего домена
- Адрес продакшен базы данных
- Защищенные секретные ключи

### Docker развертывание

Сборка и развертывание с использованием Docker:

```bash
# Сборка продакшен образа
docker build -t vertex-ar-app .

# Запуск с продакшен окружением
docker run -p 8000:8000 \
  -e ENVIRONMENT=production \
  -e DATABASE_URL=postgresql://... \
  -e SECRET_KEY=your-secret-key \
  vertex-ar-app
```

### Docker Compose развертывание

Используйте предоставленную конфигурацию docker-compose для развертывания с несколькими контейнерами:

```bash
# Развертывание с docker-compose
docker compose up -d --build
```

Подробнее см. [Deployment Guide](docs/DEPLOYMENT.md)

## 🤝 Вклад в проект

Если вы хотите внести свой вклад в развитие проекта:
1. Форкните репозиторий
2. Создайте feature ветку (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 👥 Команда разработчиков

Vertex AR Development Team

## 📞 Поддержка

При возникновении проблем:
1. Проверьте [Troubleshooting Guide](docs/TROUBLESHOOTING.md)
2. Изучите логи приложения
3. Проверьте документацию API: http://localhost:8000/docs
4. Создайте issue в репозитории
