<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/ar_background">

        <!-- AR Surface (Camera) -->
        <androidx.appcompat.widget.AppCompatSurfaceView
            android:id="@+id/surface_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- UI Overlay -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- Loading State -->
            <LinearLayout
                android:id="@+id/layout_ar_loading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:background="@drawable/ar_loading_background"
                android:elevation="8dp"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="40dp"
                android:visibility="gone"
                tools:visibility="visible">

                <!-- Spinner -->
                <com.google.android.material.progressindicator.CircularProgressIndicator
                    android:id="@+id/loading_spinner"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:indeterminate="true"
                    app:indicatorColor="@color/ar_accent"
                    app:indicatorSize="40dp"
                    app:trackThickness="3dp" />

                <!-- Title -->
                <TextView
                    android:id="@+id/text_loading_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="20dp"
                    android:fontFamily="sans-serif-medium"
                    android:gravity="center"
                    android:text="@string/ar_loading_title"
                    android:textColor="@color/ar_text_primary"
                    android:textSize="17sp" />

                <!-- Tip -->
                <TextView
                    android:id="@+id/text_loading_tip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:gravity="center"
                    android:lineSpacingMultiplier="1.3"
                    android:maxWidth="260dp"
                    android:textColor="@color/ar_text_secondary"
                    android:textSize="13sp"
                    tools:text="Обеспечьте хорошее освещение" />

            </LinearLayout>

            <!-- AR Controls (Bottom) -->
            <LinearLayout
                android:id="@+id/layout_ar_controls"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:background="@drawable/ar_controls_gradient"
                android:elevation="8dp"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingHorizontal="24dp"
                android:paddingVertical="28dp"
                android:visibility="gone"
                tools:visibility="visible">

                <!-- Action Buttons -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:orientation="horizontal">

                    <!-- Photo Button -->
                    <com.google.android.material.floatingactionbutton.FloatingActionButton
                        android:id="@+id/button_capture_photo"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginEnd="32dp"
                        android:contentDescription="@string/capture_photo"
                        android:src="@drawable/ic_camera"
                        app:backgroundTint="@color/ar_surface_elevated"
                        app:borderWidth="0dp"
                        app:elevation="4dp"
                        app:fabCustomSize="56dp"
                        app:maxImageSize="24dp"
                        app:tint="@color/ar_accent" />

                    <!-- Record Button (Large Center) -->
                    <com.google.android.material.floatingactionbutton.FloatingActionButton
                        android:id="@+id/button_record_video"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:contentDescription="@string/record_video"
                        android:src="@drawable/ic_videocam"
                        app:backgroundTint="@color/ar_accent"
                        app:borderWidth="0dp"
                        app:elevation="6dp"
                        app:fabCustomSize="72dp"
                        app:maxImageSize="28dp"
                        app:tint="@color/ar_background" />

                    <!-- Gallery Button -->
                    <com.google.android.material.floatingactionbutton.FloatingActionButton
                        android:id="@+id/button_open_gallery"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="32dp"
                        android:contentDescription="@string/photo_saved"
                        android:src="@drawable/ic_gallery"
                        app:backgroundTint="@color/ar_surface_elevated"
                        app:borderWidth="0dp"
                        app:elevation="4dp"
                        app:fabCustomSize="56dp"
                        app:maxImageSize="24dp"
                        app:tint="@color/ar_accent" />

                </LinearLayout>

                <!-- Recording Indicator -->
                <TextView
                    android:id="@+id/text_recording_indicator"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:drawablePadding="6dp"
                    android:text="@string/recording"
                    android:textColor="@color/ar_error"
                    android:textSize="13sp"
                    android:visibility="gone"
                    app:drawableStartCompat="@drawable/ic_recording_dot"
                    app:drawableTint="@color/ar_error" />

            </LinearLayout>

            <!-- Close Button (Top Right) -->
            <ImageView
                android:id="@+id/button_close_ar"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="top|end"
                android:layout_margin="16dp"
                android:background="@drawable/ar_close_button_background"
                android:contentDescription="@string/close"
                android:padding="10dp"
                android:src="@drawable/ic_close"
                app:tint="@color/ar_text_primary" />

            <!-- Error View -->
            <include
                android:id="@+id/ar_error_view"
                layout="@layout/layout_ar_error"
                android:visibility="gone"
                tools:visibility="visible" />

        </FrameLayout>

    </FrameLayout>
</layout>
