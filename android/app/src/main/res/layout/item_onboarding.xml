<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="40dp">

    <!-- Icon -->
    <ImageView
        android:id="@+id/image_onboarding"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_marginBottom="40dp"
        android:background="@drawable/onboarding_icon_background"
        android:contentDescription="@null"
        android:padding="32dp"
        android:src="@drawable/ic_qr_code_scanner"
        app:tint="@color/primary" />

    <!-- Title -->
    <TextView
        android:id="@+id/text_onboarding_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:textColor="@color/text_primary"
        android:textSize="24sp"
        android:letterSpacing="-0.02"
        tools:text="Наведите камеру на QR" />

    <!-- Description -->
    <TextView
        android:id="@+id/text_onboarding_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:lineSpacingMultiplier="1.4"
        android:textColor="@color/text_secondary"
        android:textSize="15sp"
        tools:text="Отсканируйте QR-код на картине или маркере" />

</LinearLayout>
