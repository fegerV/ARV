# Анализ системы управления видео с расписанием

## 📊 Сравнение текущей реализации с предложенной

### ✅ Что уже реализовано

1. **Базовые типы ротации:**
   - ✅ `none` - фиксированное видео
   - ✅ `sequential` - последовательное переключение
   - ✅ `cyclic` - циклическое переключение

2. **Расписание видео (VideoSchedule):**
   - ✅ Временные окна (start_time, end_time)
   - ✅ Приоритет расписания над ротацией
   - ✅ Статус расписания (active/expired)

3. **Логика выбора активного видео:**
   - ✅ Приоритет 1: Видео с активным расписанием
   - ✅ Приоритет 2: ARContent.active_video_id
   - ✅ Приоритет 3: Ротация (sequential/cyclic)
   - ✅ Приоритет 4: Fallback

4. **Уведомления:**
   - ✅ Функции отправки email/Telegram
   - ⚠️ Нет автоматической проверки истечения

### ❌ Что отсутствует

1. **Типы ротации:**
   - ❌ `date_specific` - конкретные даты (31 декабря, 14 февраля)
   - ❌ `daily_cycle` - ежедневная ротация поочередно
   - ❌ `weekly_cycle` - по дням недели
   - ❌ `monthly_cycle` - по дням месяца
   - ❌ `random_daily` - рандом каждый день с seed

2. **Архитектура:**
   - ❌ Нет единой таблицы `video_rotation_rules`
   - ❌ Нет JSONB поля `date_rules` для конкретных дат
   - ❌ `rotation_type` хранится на уровне Video, а не ARContent
   - ❌ Нет поля `rotation_state` в модели ARContent (используется, но не определено в модели)

3. **Приоритет правил:**
   - ❌ Нет поддержки приоритетных дат (праздники > основное)
   - ❌ Нет проверки конкретных дат перед ротацией

4. **Уведомления:**
   - ❌ Нет автоматической задачи для проверки истечения
   - ❌ Нет проверки за 7 дней до окончания
   - ❌ Нет связи с ARContent.expires_at (используется только subscription_end)

5. **UI/API:**
   - ❌ Нет API для тестирования расписания (`/test-schedule`)
   - ❌ Нет календарного представления расписания

## 🔧 Критические проблемы

### 1. Отсутствие поля `rotation_state` в модели

**Проблема:** Код использует `ar_content.rotation_state`, но поле не определено в модели `ARContent`.

**Решение:** Добавить миграцию:
```python
rotation_state = Column(Integer, default=0, nullable=True)
```

### 2. Неправильная архитектура ротации

**Проблема:** 
- `rotation_type` хранится на уровне `Video`, а должен быть на уровне `ARContent` или правила
- Нет единой точки управления правилами ротации

**Текущая структура:**
```
ARContent
  └─ Video (rotation_type: "none"|"sequential"|"cyclic")
  └─ VideoSchedule (start_time, end_time)
  └─ VideoRotationSchedule (не используется в основном коде)
```

**Предложенная структура:**
```
ARContent
  └─ VideoRotationRule (rotation_type, date_rules, video_sequence)
  └─ Video (is_default, rotation_weight)
  └─ VideoSchedule (для временных окон)
```

### 3. Отсутствие поддержки конкретных дат

**Проблема:** Нет способа указать "31 декабря → Новогодняя.mp4"

**Текущее решение:** Использовать `VideoSchedule` с конкретными датами, но это неудобно для ежегодных событий.

**Нужно:** JSONB поле `date_rules` с поддержкой:
- Конкретных дат: `{"date": "2025-12-31", "video_id": 2}`
- Ежегодных дат: `{"month": 12, "day": 31, "video_id": 2}`

## 📋 Рекомендации по реализации

### Этап 1: Критические исправления

1. **Добавить `rotation_state` в ARContent:**
```python
# alembic migration
def upgrade():
    op.add_column('ar_content', 
        sa.Column('rotation_state', sa.Integer(), nullable=True, server_default='0'))
```

2. **Исправить логику ротации:**
   - Перенести `rotation_type` на уровень ARContent или создать VideoRotationRule
   - Использовать `VideoRotationSchedule` вместо хранения типа в Video

### Этап 2: Расширение функциональности

1. **Добавить поддержку конкретных дат:**
```python
# Расширить VideoRotationSchedule
date_rules = Column(JSON().with_variant(JSONB, "postgresql"), nullable=True)
# Формат: [{"date": "2025-12-31", "video_id": 2, "recurring": true}]
```

2. **Реализовать новые типы ротации:**
```python
async def get_active_video(ar_content_id: int, db: AsyncSession, override_date: date = None):
    today = override_date or date.today()
    
    # 1. Проверить конкретные даты (приоритет)
    date_video = await check_date_rules(rule, today)
    if date_video:
        return date_video
    
    # 2. Проверить расписание
    scheduled_video = await check_schedules(ar_content_id, db, today)
    if scheduled_video:
        return scheduled_video
    
    # 3. Применить ротацию по типу
    match rule.rotation_type:
        case "daily_cycle":
            return await get_daily_cycle_video(rule, today)
        case "weekly_cycle":
            return await get_weekly_cycle_video(rule, today)
        case "random_daily":
            return await get_random_video(rule, today)
        # ...
```

3. **Автоматические уведомления:**
```python
# app/background_tasks/expiry_checker.py
@shared_task
async def check_expiring_content():
    """Ежедневная проверка истекающего контента"""
    tomorrow = datetime.now() + timedelta(days=7)
    
    # Проверить ARContent с expires_at
    expiring = await db.execute(
        select(ARContent)
        .where(ARContent.expires_at == tomorrow)
        .where(ARContent.status == "active")
    )
    
    for content in expiring.scalars():
        await send_expiry_notification(content)
```

### Этап 3: UI/UX улучшения

1. **Календарь расписания:**
   - Визуализация активных видео по датам
   - Редактирование конкретных дат

2. **Тестирование расписания:**
   - API endpoint `/test-schedule?date=2025-12-31`
   - Предпросмотр календаря

## 🎯 Приоритеты реализации

### Высокий приоритет (критично):
1. ✅ Добавить `rotation_state` в ARContent
2. ✅ Исправить использование `VideoRotationSchedule`
3. ✅ Добавить поддержку конкретных дат (date_specific)

### Средний приоритет (важно):
4. ✅ Реализовать `daily_cycle`, `weekly_cycle`
5. ✅ Реализовать `random_daily` с seed
6. ✅ Автоматические уведомления за 7 дней

### Низкий приоритет (желательно):
7. ✅ Календарный UI
8. ✅ API для тестирования расписания
9. ✅ Поддержка `monthly_cycle`

## 📝 Выводы

**Текущая реализация:**
- ✅ Работает для базовых сценариев (1 видео, sequential, cyclic)
- ✅ Поддерживает временные окна через VideoSchedule
- ⚠️ Не поддерживает сложные сценарии (праздники, ежедневная ротация)
- ❌ Архитектурно не соответствует предложенному дизайну

**Рекомендация:**
1. Сначала исправить критические проблемы (rotation_state, архитектура)
2. Затем постепенно добавлять новые типы ротации
3. Интегрировать автоматические уведомления
4. Улучшить UI для управления расписанием

**Оценка сложности:**
- Критические исправления: 2-3 дня
- Расширение функциональности: 1-2 недели
- UI улучшения: 1 неделя
