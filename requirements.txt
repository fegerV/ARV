# Основные зависимости
# Security: 0.109.0 pulled starlette<0.36 (CVE-2024-47874 multipart DoS).
fastapi>=0.115.0
# Security: starlette 0.40.0 fixes CVE-2024-47874 (multipart/form-data DoS).
starlette>=0.40.0
uvicorn[standard]==0.27.0
gunicorn>=21.0

# База данных
sqlalchemy[asyncio]==2.0.41
asyncpg==0.31.0
aiosqlite==0.20.0
alembic==1.13.1

# Валидация и сериализация (orjson нужен для ORJSONResponse в FastAPI)
orjson>=3.9.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dateutil==2.8.2
email-validator==2.1.0
# Security: <0.0.18 vulnerable to CVE-2024-24762 (ReDoS) and
# CVE-2024-53981 (DoS via malformed multipart boundary).
python-multipart>=0.0.18

# Аутентификация и авторизация
# Security: 3.3.0 vulnerable to CVE-2024-33663 (algorithm confusion) and
# CVE-2024-33664 (JWT bomb / DoS).
python-jose[cryptography]>=3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# Обработка файлов и медиа
numpy>=1.26.0,<2.0.0  # OpenCV 4.9 несовместим с NumPy 2.x
ffmpeg-python==0.2.0
pillow>=11.3.0
qrcode[pil]==7.4.2
opencv-python-headless==4.9.0.80
aiofiles==23.2.1

# AR маркеры
# mind_ar==1.0.0  # Commented out as it's not available

# QR коды
qrcode[pil]==7.4.2

# Обработка изображений
pillow>=11.3.0

# Обработка видео
# NOTE: do NOT install both opencv-python and opencv-python-headless — they
# provide the same `cv2` module and overwrite each other. The headless build is
# used (no GUI/X11 deps), so the non-headless package is intentionally omitted.

# HTTP клиент
httpx==0.26.0

# Логирование
structlog==24.1.0
python-json-logger==2.0.7

# Мониторинг
psutil==5.9.8
prometheus-client==0.19.0

# Планировщик задач
apscheduler>=3.10,<4

# Утилиты
python-dotenv==1.0.0
# Security: <3.1.5 vulnerable to CVE-2024-34064 (XSS via xmlattr),
# CVE-2024-56201 (sandbox escape) and CVE-2024-56326 (sandbox escape).
jinja2>=3.1.5
itsdangerous==2.2.0
redis[hiredis]>=4.5

# Тестирование
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Форматирование и линтинг
black==23.12.1
isort==5.13.2
flake8==7.0.0
mypy==1.8.0
slowapi==0.1.9
